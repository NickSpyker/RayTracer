/*
** EPITECH PROJECT, 2022
** Launcher
** File description:
** Launcher
*/
#include "RaytracerApplication.hpp"

/*int main(int ac, char **av) {
  if (ac != 2) {
    std::cerr << "Usage: " << av[0] << " [scene]" << std::endl;
    return 84;
  }
  if (std::string(av[1]) == "-h" || std::string(av[1]) == "--help") {
    std::cout << "USAGE: " << av[0] << " <SCENE_FILE>" << std::endl;
    std::cout << "  SCENE_FILE: scene configuration" << std::endl;
    return 0;
  }
  std::string sceneFile(av[1]);

  RayTracer::RaytracerApplication app(sceneFile);
  int ret;
  try {
    ret = app.run();
  } catch (const std::exception &e) {
    std::cerr << e.what() << std::endl;
    return 84;
  }
  return ret;
}*/
